{{ form_start(form) }}
  <div class="form-control">
    <i class="fa fa-calendar" style = "color"></i> {{ form_label(form.description) }}
    {{ form_widget(form.description) }}</div>

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
